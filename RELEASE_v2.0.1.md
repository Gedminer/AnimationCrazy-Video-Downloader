# AnimationCrazy-Video-Downloader v2.0.1

巴哈姆特動畫瘋下载器 v2 —— 浏览器自动取流 + 批量下载 + 无头后台。

## 新增：双语配音支持

部分番剧在动画疯上同时提供「原音(日语)」与「中文配音」两种音轨。v2.0.1 让下载器能识别并分别处理这两种配音：

- **`info` 分块显示**：双语作品按 `【原音(日语)】` / `【中文配音】` 分别列出剧集；单语作品显示不变。导出的 JSON 额外包含 `bilingual` / `audioLabels` / `groups` 及每集的 `group` / `audio` 字段。
- **`dl` / `batch` 可选配音**：新增 `--audio` / `--lang` 参数。
  - 不写时：`dl <sn>` 默认下载该 sn **所属语种**；`batch` 默认下载 `config.ini` 中 `default_audio` 指定的配音。
  - 分组键 `0` / `3` 直接指定某一配音；名称片段 `中文` / `原音` / `日语` 按标签匹配；`all` 表示两种配音都下载（文件名带 `[原音(日语)]` / `[中文配音]` 区分，避免冲突）。
  - 交互模式批量下载会在未指定 `--audio` 时先让你选择配音。
- **新增配置项 `default_audio`**（默认 `原音(日语)`）：决定批量下载未指定配音且非交互(`-y`)时的默认配音。

## 其他改进

- 作品名解析更稳健：剥离动画疯接口在双语作品标题末尾附加的 `[集数]` / `[配音]` 等方括号后缀，避免目录名被污染。
- `info --dump-json` 现在会自动创建输出文件的父目录。

## 使用方法

详见仓库内 `使用教程.md` 与 `README.md`。双语示例：

```bash
ac-dl.py info 50633                      # 查看双语作品各配音信息
ac-dl.py dl 50633                        # 默认下载该 sn 所属语种（中文配音）
ac-dl.py dl 50633 --audio 0              # 指定原音(日语)
ac-dl.py dl 50633 --audio all            # 两种配音都下
ac-dl.py batch 50633 --audio 中文配音 --select 1-8   # 批量指定配音
```

## 安装

详见 `README.md`。核心依赖：

- Python 3.10+
- Playwright（Chromium）
- `N_m3u8DL-RE`（已随仓库附带于 `tools/N_m3u8DL-RE/`）

## 说明

本工具依赖浏览器自动化，请仅用于下载你拥有合法观看 / 下载权限的内容。
