# 动画疯下载器 · 实战使用教程

> 面向第一次使用的新手。目标：5 分钟内跑通「登录 → 看剧集 → 下载第一集 → 批量下载整部」。
> 更完整的参数与原理见 [README.md](README.md)。

---

## 0. 你将会得到什么

以《GRAND BLUE 碧藍之海 3》（作品 sn = `50646`）为例，最终在 `downloads/GRAND BLUE 碧藍之海 3/` 下得到每集一个 `mp4`（约 720p）。

---

## 1. 准备环境（只需一次）

```bash
# 安装 Python 依赖
pip install -r requirements.txt

# 安装 Playwright 浏览器内核（仅首次）
playwright install chromium
```

- **下载器已打包**：`tools/N_m3u8DL-RE/N_m3u8DL-RE.exe`，无需另行下载。
- **ffmpeg（强烈推荐）**：用于封装为 `mp4`。把它放到仓库根目录，或确保在 `PATH` 中；缺失时退化为 `.ts` 输出。
- 网络需能访问 `ani.gamer.com.tw`，且你有一个**已登录动画疯**的账号。

---

## 2. 第一次登录（持久化，之后免登录）

```bash
python ac-dl.py login
```

浏览器会自动打开动画疯登录页，手动登录一次即可。登录态保存在本地 `.auth/`，**不会提交到仓库**。
退出登录用 `python ac-dl.py logout`。

> 若你已用 Chrome 登录动画疯，可改用 CDP 模式复用现有浏览器：
> 先以 `chrome.exe --remote-debugging-port=9222 --user-data-dir="%LOCALAPPDATA%\ac-dl-cdp"` 启动，
> 再运行 `python ac-dl.py login --mode cdp`。

---

## 3. 查看作品剧集列表

```bash
python ac-dl.py info 50646
```

输出类似：

```
GRAND BLUE 碧藍之海 3   共 12 集
 #   集数       sn
 1   第01集     50647
 2   第02集     50648
 ...
```

也可导出为 JSON 便于批量脚本处理：`python ac-dl.py info 50646 --dump-json eps.json`。

---

## 4. 下载第一集（试水）

```bash
python ac-dl.py dl 50646
```

默认下载该作品**最新一集**（即第 12 集）。程序会：
1. 打开播放页、等待广告播完；
2. 自动锁定正片流，捕获 `m3u8` / 密钥 / `IV`；
3. 调用 `N_m3u8DL-RE` 下载并封装为 `mp4`。

下载完成后在 `downloads/GRAND BLUE 碧藍之海 3/` 看到 `第12集.mp4`。

---

## 5. 批量下载整部

```bash
# 下载第 1~8 集
python ac-dl.py batch 50646 --select 1-8

# 非交互下载全部集（适合后台）
python ac-dl.py batch 50646 --select all -y
```

**选集表达式速查**：

| 写法 | 含义 |
|---|---|
| `all` / 留空 | 全部集 |
| `1-12` | 第 1 到第 12 集 |
| `1,3,5-8` | 第 1、3、5、6、7、8 集 |
| `8-` 或 `8+` | 从第 8 集到最后一集 |
| `-6` | 第 1 到第 6 集 |
| `last` | 仅最新一集 |

> 批量默认**串行 + 集间冷却**，降低账号风控。`config.ini` 里 `cooldown` / `task_retry` 可调。

---

## 6. 后台无头下载（推荐给"挂着跑"）

```bash
# 无头 + 弹独立控制台窗口看实时进度（Windows）
python ac-dl.py batch 50646 --select all -y --headless
```

- `headless=True` 会用**完整 Chromium**（非残缺的 headless_shell），广告自动推进、取流正常。
- 想静默跑（不弹窗，写日志到 `<保存名>.re.log`）：在命令前加 `ACDL_SHOW_CLI=0`。

---

## 7. 常见问题速查

**Q：卡在广告 / 取不到流？**
A：确认用的是完整 Chromium（v2 已自动处理）；仍异常时改用 `--mode cdp` 或带头模式排查登录态。

**Q：解密报 `Padding is invalid`？**
A：通常是清晰度切换导致密钥/IV 错位。v2 已用 `_locked` 锁定已验证清晰度规避；若仍出现，在 `config.ini` 显式写 `resolution = 720` 再跑。

**Q：输出是 `.ts` 而不是 `.mp4`？**
A：没检测到 ffmpeg。安装 ffmpeg 并放入 `PATH` 或仓库根目录。

**Q：重复跑会不会又下一遍？**
A：`skip_existing = True`（默认）会跳过已存在的集。

---

## 8. （可选）把项目推送到你自己的 GitHub

本项目附带 `push-to-github.ps1`——一条**令牌不经过任何人**的 PowerShell 推送脚本（令牌在你本机输入、不回显，推送后自动从 remote URL 抹掉）。

用法（在你本机、仓库根目录的 PowerShell 中）：

```powershell
powershell -ExecutionPolicy Bypass -File push-to-github.ps1
```

前置：① 在 GitHub 建好空仓库（建议 Private）；② 生成 Fine-grained PAT（Repository access 选该仓库，Permissions → Contents: Read and write）；③ 按提示填用户名与令牌即可。
详细注释见脚本头部。
